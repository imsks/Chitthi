# Chitthi web

Next.js app for Chitthi. Run it with the **root** project:

```bash
cd ..
docker compose up --build
```

UI: http://localhost:3000 · API: http://localhost:8080

See the repository [readme.md](../readme.md) for setup, migrations, and API details.
